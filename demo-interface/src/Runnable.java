public interface Runnable {// Interface is a contract

  // Before Java 8 - 100% Abstraction
  void run(); // implicitly public & abstract, must no private

  // void eat(); // implicitly public & abstract, must no private

}
